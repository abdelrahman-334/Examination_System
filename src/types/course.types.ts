export interface ICourse {
    courseId: number;
    courseName: string;
    courseLength: number;
}

export interface ICreateCourseDTO {
    courseId: number;
    courseName: string;
    courseLength: number;
}